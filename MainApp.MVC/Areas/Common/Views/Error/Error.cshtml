@using MainApp.MVC.Helpers;
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@{
    Layout = "../Shared/_LayoutPage.cshtml";
    ViewData["Title"] = "Error";
}
<div class="abs-center wd-xl w-100">
    <!-- START card-->
    <div class="text-center mb-4">
        <div class="mb-3">
            <em class="fa fa-wrench fa-5x text-secondary"></em>
        </div>
        <p class="lead text-secondary m-0">@DbResHtml.T("Oh! Something went wrong","Resources") <i class="far fa-frown"></i></p>
        <p class="text-muted">@DbResHtml.T("Don't worry, we're now checking this.","Resources")</p>
        <p>@DbResHtml.T("In the meantime, please try one of those links below or come back in a moment","Resources")</p>
    </div>
    <ul class="list-inline text-center text-sm mb-4">
        <li class="list-inline-item">
            <a class="btn bg-gradient-secondary" href="~/@Configuration["DomainSettings:MainApp"]">@DbResHtml.T("Home", "Resources")</a>
        </li>
    </ul>
    <div class="p-3 text-center">
        <span class="mr-2">&copy;</span>
        <span>2023</span>
        <span class="mr-2">-</span>
        <span>UNICEF</span>
    </div>
</div>