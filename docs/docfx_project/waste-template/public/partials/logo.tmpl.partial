<a class="navbar-brand" href="{{_rel}}index.html">
  <div class="logo-container">
    <svg width="40" height="40" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" class="logo-svg">
      <!-- Drone shape -->
      <path d="M20 5 L35 15 L35 25 L20 35 L5 25 L5 15 Z" 
            stroke="var(--green-active)" 
            stroke-width="2" 
            fill="none" />
      <!-- Camera lens -->
      <circle cx="20" cy="20" r="6" 
              stroke="var(--green-active)" 
              stroke-width="2" 
              fill="none" />
      <!-- Detection indicator -->
      <path d="M20 10 L20 14 M10 20 L14 20 M20 26 L20 30 M26 20 L30 20" 
            stroke="var(--green-success)" 
            stroke-width="2" />
    </svg>
    <span class="logo-text">{{_appTitle}}{{^_appTitle}}Waste Detection{{/_appTitle}}</span>
  </div>
</a>

<style>
/* Logo styles - include in main.css */
.logo-container {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.logo-svg {
  transition: transform 0.3s ease;
}

.logo-svg:hover {
  transform: scale(1.1);
}

.logo-text {
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--white);
  white-space: nowrap;
}

@media (max-width: 768px) {
  .logo-text {
    font-size: 1rem;
  }
  
  .logo-svg {
    width: 32px;
    height: 32px;
  }
}

/* Animation for detection indicators */
@keyframes pulse {
  0% { opacity: 0.4; }
  50% { opacity: 1; }
  100% { opacity: 0.4; }
}

.logo-svg path:last-child {
  animation: pulse 2s infinite;
}
</style>